# Implementation Plans

Generated by the improve skill on 2026-08-24 (audit at commit `e64f9ca`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done unless the reviewer maintains the index.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Store resilience: backup recovery, folder and URL migrations | P1 | M | — | DONE |
| 002 | Fix File These for Me: respect manual filing, report, batch | P1 | S | 001 | TODO |
| 003 | URL and export hygiene: apostrophe trim, markdown escaping | P1 | S | — | DONE |
| 004 | Lightness quick wins: thumbnail cache, single-pass counts | P1 | S | — | TODO |
| 005 | Centralize the http(s) scheme gate at the store boundary | P2 | S | 001 | TODO |
| 006 | Enrichment collision: merge instead of delete, close the race | P2 | M | 001, 005 | TODO |
| 007 | OpenGraph parser: quote-aware tags, property over name | P2 | M | — | DONE |
| 008 | Folder identity: one test that catches lockstep misses | P2 | M | — | TODO |
| 009 | Lightness deep cuts: single fetch per enrichment, chunked reads | P2 | M | 006 | TODO |
| 010 | Test hygiene: real pick assertions, temp cleanup, timing flags | P3 | S | — | TODO |
| 011 | Investigate: AppleScript threading and stacked rename alerts | P3 | S | — | TODO |
| 012 | Publication pack: docs truth, CLAUDE.md, VERSION, CI package job | P2 | M | 001-010 | TODO |
| 013 | Spike: Notes importer v2 design (per-folder, incremental) | P3 | M | — | TODO |
| 014 | Browser bookmarks import and file export | P3 | M | 003 | TODO |
| 015 | Spike: Share Extension packaging feasibility | P3 | M | — | TODO |
| 016 | Upgrade LLM categorizer to @Generable guided generation | P3 | M | — | BLOCKED (needs an Xcode 26 build machine) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002, 005, 006 touch `BookmarkStore.swift`; 001 lands first so their excerpts stay true.
- 006 builds on 005's store-level scheme gate and 001's migration-aware init.
- 009 refactors the fetch path 006's tests exercise; land 006 first.
- 012 documents behavior the earlier plans change; it goes last of the fix wave.
- 014 feeds imported URLs through the capture gate hardened in 003/005.

## Findings considered and rejected

- pick()/search() full scans: inside the tested benchmark budgets; revisit only if the section-12 benchmark tests miss targets.
- Per-site fetchers for Instagram/YouTube/Reddit: Instagram is spec-rejected (login wall); the others are served by the generic OpenGraph path.
- Favicon fetching: no repo evidence of need; requires a per-site fetch design decision first.
- CI build caching: macOS runner and a small package; not worth the config.
- Private-IP fetch blocking (SSRF hardening): real pattern, but a silent block breaks intranet bookmarks users genuinely want; parked until a visible policy UX exists.
- swift test does not compile the app target: disproved in vetting; `.build/debug/Dogear` is produced by `swift test`.
